[package]
name = "api"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { workspace = true, features = ["fullstack"] }
wco = { workspace = true }
serde = { version = "1", features = ["derive"] }

[features]
server = ["dioxus/server"]
